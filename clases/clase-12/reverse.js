'use strict';

// Array:
let let frutas = ['banana', 'limón', 'manzana', 'mango'];

// Aplicación del méotodo:
frutas.reverse();

// Resultados:
console.info('frutas:', frutas);
console.info('-----');

// Aplicación del méotodo:
frutas.sort();

// Resultados:
console.info('frutas:', frutas);
console.info('-----');

// Aplicación del méotodo:
frutas.reverse();

// Resultados:
console.info('frutas:', frutas);